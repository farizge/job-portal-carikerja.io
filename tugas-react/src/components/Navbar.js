import React from "react";

const Navbar = () => {
    return (
        <nav className="navbar">
            <h2>Kelas Reactjs</h2>
            <div className="profile"></div>
        </nav>
    );
};

export default Navbar;
